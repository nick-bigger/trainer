# Copy to .env.local and fill in. Same Turso credentials are used by the browser
# (VITE_ prefix makes them available to the client) and the coach server.
VITE_TURSO_DB_URL=libsql://trainer-<org>.turso.io
VITE_TURSO_AUTH_TOKEN=<token>
